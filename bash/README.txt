bash script

download_filter_script (download ccmp files from HTTPS, only chose a lat lon box, and years interval, it is downloading, then subsetting, then cancelling what is not needed and keep the subset file)
